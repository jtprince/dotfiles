# the venv is already included in starship
export VIRTUAL_ENV_DISABLE_PROMPT=1


if command -v rbenv &> /dev/null; then
    eval "$(rbenv init - zsh)"
fi

eval "$(starship init zsh)"

export PATH="/opt/homebrew/opt/perl/bin:$PATH"
