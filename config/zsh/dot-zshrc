# .zshrc — sourced for interactive shells

export VIRTUAL_ENV_DISABLE_PROMPT=1

if command -v rbenv &> /dev/null; then
    eval "$(rbenv init - zsh)"
fi

# Load sheldon plugins (interactive-only; PATH is set by .zprofile)
eval "$(sheldon source)"

eval "$(starship init zsh)"
