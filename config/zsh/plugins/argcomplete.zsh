
# pip install argcomplete (or equivalent command for your env)
# uv pip install --python <whatever venv> argcomplete
autoload -U bashcompinit && bashcompinit
eval "$(register-python-argcomplete cheat)"
