
alias libreoffice_to_pdf="libreoffice --headless --convert-to pdf"

# hosts
alias gosuper="ssh -Y $SUPER"
alias gobigmem="ssh -Y $BIGMEM"
RDESKTOPOPTS="-r clipboard:PRIMARYCLIPBOARD"
alias kelso="rdesktop $RDESKTOPOPTS -g workarea $KELSOIP"
alias kelso1280="rdesktop $RDESKTOPOPTS -g 1280x1024 $KELSOIP"
alias kelso90="rdesktop $RDESKTOPOPTS -g 90% $KELSOIP"
alias kelso45="rdesktop $RDESKTOPOPTS -g 45% $KELSOIP"
alias kelso1920="rdesktop $RDESKTOPOPTS -g 1920x1200 $KELSOIP"

alias sudoe="sudo -E"
alias mp="ncmpcpp"

# apt-get
alias apti='sudo apt-get install'
alias aptinstall='sudo apt-get install'
alias apts='aptitude search'
#alias apts='apt-cache search'  # not enough info
alias aptsearch='aptitude search'
alias aptr='sudo aptitude remove --purge-unused'
alias aptremove='sudo aptitude remove --purge-unused'
alias aptshow='apt-cache show'
alias aptaddkey='sudo apt-key adv --recv-keys --keyserver keyserver.ubuntu.com'

alias mount_wd15="gvfs-mount smb://wd15/USB_Storage"

alias dependenciesr="apt-cache --recurse depends"
alias dependencies="apt-cache depends"
alias depends_on="apt-cache rdepends"

# extraction
alias utar='dtrx -n'

alias wine32="export WINEARCH=win32; export WINEPREFIX=~/.wine32; wine"
alias winecfg32="export WINEARCH=win32; export WINEPREFIX=~/.wine32; winecfg"
alias wine64="export WINEARCH=win64; export WINEPREFIX=~/.wine64; wine"
alias winecfg64="export WINEARCH=win64; export WINEPREFIX=~/.wine64; winecfg"

# navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# ruby
alias be="bundle exec"

# git
alias gist="git status"

# misc
alias ls="ls --color=auto"
alias bunzip='bunzip2'
alias bzip='bzip2'
alias dl='gvfs-trash'
#alias presentation="xrandr --output VGA1 --mode 1024x768 --same-as primary"
#alias presentation_hidden="xrandr --output VGA1 --mode 1024x768 --right-of primary"
#alias unpresentation="xrandr --output primary"

alias atlasinclude="export C_INCLUDE_PATH=/usr/include/atlas && export CPLUS_INCLUDE_PATH=/usr/include/atlas"
alias includeatlas=atlasinclude

# spelling mistakes
alias sl="ls"
alias givm="gvim"
alias gvmi="gvim"
alias gimv="gvim"
alias mdkir="mkdir"
alias eixt="exit"

#alias msword='wine32 "/home/jtprince/.wine32/drive_c/Program Files/Microsoft Office/Office12/WINWORD.EXE"'

# media
#alias tmux="tmux -f ~/.config/tmux/config"
alias vlisten="vlc --intf rc -Vdummy"

# latex
alias latexmk="latexmk -pdf -xelatex"

# grep
alias rgrep="grep -r"
alias rgreptext="grep -r --binary-files=without-match"

alias newterm="urxvtc"

# MAC retina via HDMI to 1920x1080 (correct for overscan)
alias overscancorrect="nvidia-settings -a CurrentMetaMode=\"DFP-3:2880x1800+0+0, DFP-2: 1920x1080+2880+0 { ViewPortOut=1820x1022+50+29 }\""

alias gitcim="gitci"
