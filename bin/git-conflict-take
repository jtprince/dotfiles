#!/usr/bin/env python

import argparse
from collections import namedtuple

Match = namedtuple('Match', 

def _prep_match(match_str):

description = """takes ours or theirs unmerged paths"""

parser = argparse.ArgumentParser(description=description)
parser.add_argument("stage", nargs='*', choices=['ours', 'theirs'])
parser.add_argument("-m", "--match", help="*only* for paths that match the regex. path[:stage]")
args = parser.parse_args

_prep_match(args.match)

if args
