#!/usr/bin/env python

import argparse

description = """takes ours or theirs merge branches"""

parser = argparse.ArgumentParser(description=description)
parser.add_argument("-m", "--match", help="*only* for paths that match")
args = parser.parse_args

if args

