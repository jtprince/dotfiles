#!/bin/bash

ARG1=${1:-master}

if [[ -z $(git status -s -uno) ]]; then
    git fetch -p
    git merge "origin/$ARG1"
else
    echo "********************************************************"
    echo "** Will not merge while you have uncommitted changes! **"
    echo ""
    echo "uncommitted:"
    git status -s
    echo "********************************************************"
fi
