#!/usr/bin/env python

# Install neovim and related dependencies

import subprocess

TO_INSTALL = [
    "neovim",
    "isort",
    "pylint",
    "flake8",
    "black",
    "ipdb",
]

cmd = ["pip", "install", *TO_INSTALL]

subprocess.run(cmd)
